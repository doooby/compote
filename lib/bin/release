#!/bin/bash
set -e

source ops/libops.sh
libops_print "release of $stack_name started"

libops_script_run build.sh

libops_script_run release.sh

libops_print "restarting services"
bin/compose up -d

libops_print "release of $stack_name finished"
